# MongoDB Connection
# Pour le développement local, utilisez MongoDB installé localement
MONGODB_URI=mongodb://localhost:27017/cotitra

# Pour la production avec MongoDB Atlas
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# Email Service Configuration
# Choisir le provider : 'gmail' ou 'resend' (défaut: resend)
EMAIL_PROVIDER=resend

# --- Option 1: Resend (nécessite un nom de domaine) ---
# Obtenir une clé API sur https://resend.com
RESEND_API_KEY=your_resend_api_key_here

# --- Option 2: Gmail (gratuit, pas de domaine requis) ---
# Pour utiliser Gmail :
# 1. Activer la validation en 2 étapes sur votre compte Gmail
# 2. Générer un mot de passe d'application sur https://myaccount.google.com/apppasswords
# 3. Décommenter les lignes ci-dessous et renseigner vos informations
# GMAIL_USER=votreemail@gmail.com
# GMAIL_APP_PASSWORD=votre_mot_de_passe_application_google

# Common to all providers
FROM_EMAIL=noreply@votredomaine.com

# Application URL (pour les liens dans les emails)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# NextAuth Configuration
# Générer une clé secrète avec: openssl rand -base64 32
NEXTAUTH_SECRET=your_secret_key_here_generate_with_openssl_rand_base64_32
NEXTAUTH_URL=http://localhost:3000
